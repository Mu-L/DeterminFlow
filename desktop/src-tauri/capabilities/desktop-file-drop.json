{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-file-drop",
  "description": "Allow the bundled loopback UI to receive native file-drop paths.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": ["core:event:default"]
}
